{
  "name": "puzzle-game",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && node server.js",
    "build": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
